# Copy to .env and fill in after registering a CONFIDENTIAL app in CTFd admin
# (Admin -> OAuth2 Apps -> Register application), with redirect URI:
#   http://localhost:5000/callback
# These are consumed by the optional `demo-client` service in docker-compose.yml.
OAUTH_CLIENT_ID=
OAUTH_CLIENT_SECRET=
